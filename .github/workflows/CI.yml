name: CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-and-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    runs-on: ${{ matrix.os }}

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up CMake (latest)
        uses: lukka/get-cmake@latest

      # Linux: Clang-18 for best C++23 modules support
      - name: Install Clang-18 (Ubuntu only)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-18 libc++-18-dev libc++abi-18-dev
          echo "CC=clang-18" >> $GITHUB_ENV
          echo "CXX=clang++-18" >> $GITHUB_ENV

      # Windows: Activate MSVC
      - name: Setup MSVC (Windows only)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure CMake
        run: |
          cmake -B build \
                -G Ninja \
                -DCMAKE_BUILD_TYPE=Release \
                -DDISCADELTA_SAMPLES=ON

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Run samples (verify execution)
        run: |
          echo "=== Running samples on ${{ runner.os }} ==="
          ./build/ch01_basic
          ./build/ch02_constraints
          ./build/ch03_optimized
          ./build/ch04_placing
